apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway-code-detective.fullname" . }}
  labels:
    {{- include "api-gateway-code-detective.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-gateway-code-detective.selectorLabels" . | nindent 4 }}
