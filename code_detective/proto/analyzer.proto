syntax = "proto3";

package analyzer;

service Analyzer {
  rpc AnalyzeContract (AnalyzeRequest) returns (AnalyzeResponse);
}

message AnalyzeRequest {
  string features_json = 1;
}

message AnalyzeResponse {
  string vulnerabilities_json = 1;
}
